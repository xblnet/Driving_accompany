version: "3.9"

services:

  app:
    image: driving_accompany
    build:
      context: ./driving_accompany
      dockerfile: Dockerfile
    restart: always

  nginx:
    image: nginx
    build: 
      context: ./nginx
      dockerfile: Dockerfile
    restart: always
    #ports:
      #- "80:80"
    deploy:
      mode: replicated
      replicas: 3
    