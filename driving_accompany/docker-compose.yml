version: "3.9"

services:

  app:
    image: driving_accompany
    build: ./driving_accompany
    restart: always
    expose:
      - 8080

  nginx:
    image: nginx
    build: ./nginx
    restart: always
    ports:
      - "80:80"
    